<%= form_for(logging) do |f| %>
  <% if logging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(logging.errors.count, "error") %> prohibited this logging from being saved:</h2>

      <ul>
      <% logging.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <%= f.text_field :password %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
